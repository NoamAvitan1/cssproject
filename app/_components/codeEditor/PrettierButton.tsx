'use client'
type Props = {
    instance: any
    code: string
};

export const PrettierButton = (props: Props) => {

  return (
    <button className="font-extrabold">
        P
    </button>
  );
};
