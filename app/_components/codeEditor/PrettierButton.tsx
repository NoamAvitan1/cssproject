'use client'
type Props = {
    instance: any
    code: string
};

export const PrettierButton = (props: Props) => {
console.log(prettier)
  return (
    <button className="font-extrabold">
        PRETTIER
    </button>
  );
};
